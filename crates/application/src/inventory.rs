//! `InventoryService` — read-only queries: `list` (snapshots/backups/all),
//! `stats`, and `list-drives`. Depends only on `SubvolumeRepository` and
//! `DriveDiscoveryPort`. See `documentation/01-phases-design-v2.md` Phase 3.
//
// TODO (Phase 1 list-drives; Phase 3 list/stats).
